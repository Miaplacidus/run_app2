<%= form_for(@join_request) do |f| %>
  <% if @join_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@join_request.errors.count, "error") %> prohibited this join_request from being saved:</h2>

      <ul>
      <% @join_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cicle_id %><br>
    <%= f.number_field :cicle_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :accepted %><br>
    <%= f.check_box :accepted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
